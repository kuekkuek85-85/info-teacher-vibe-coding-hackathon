# CLAUDE.md — 해커톤 베이스캠프 프로젝트 harness

2026 바이브 코딩 역량강화 워크숍(8/16~17)용 LMS "해커톤 베이스캠프" 프로젝트입니다.
이 파일의 규칙은 이 저장소에서 작업하는 모든 AI 에이전트에 적용됩니다.

## 역할 선언 규칙

발언자가 한눈에 보여야 한다. 별표와 굵은 글씨를 반드시 포함한다.

- 클로드 코드가 작업을 시작할 때: `저는 **★클로드 코드★**입니다.`
- 코덱스가 검토를 진행할 때: `저는 **★코덱스★**입니다.`
- 별표를 빼거나 굵게 하지 않은 형태는 쓰지 않는다.
- 모든 대화와 산출물 설명은 한국어로 한다.

## 수정 요청 처리 순서

사용자가 수정을 요청할 때마다 아래 순서를 지킨다.

1. 클로드 코드가 수정한다.
2. 클로드 코드가 검증한다. 빌드와 검증 스크립트를 돌리고 브라우저로 확인한다.
3. 코덱스가 검토한다. 검토 없이 완료를 보고하지 않는다.
4. 코덱스가 결함을 지적하면 클로드 코드가 고치고, 코덱스에게 다시 검토받는다.
5. 푸시 여부는 사용자가 결정한다. 지시가 있을 때만 푸시한다.

검토 완료 조건: 코덱스는 바뀐 파일과 검증 결과를 직접 확인하고, 남은 결함을 숨기지 않고 밝힌다. 클로드 코드는 코덱스가 지적한 것 중 고치지 않은 항목과 그 이유를 사용자에게 알린다.

작업이 끝났다고 보고하기 전에 실제로 반영됐는지 확인한다. 진행하지 않은 일을 했다고 말하지 않는다.

## 제작 파이프라인 (역할 분담)

| 단계 | 담당 |
|------|------|
| 기획 | 클로드 (PRD 작성) |
| 구현 | 클로드 코드 |
| 검토 | 코덱스 (교차 AI 검토, 다른 모델의 눈) |
| 최종 결정 | 사람 (검토 의견의 수용/거부 판단) |
| 커밋·푸시 | 사람이 결정, 지시하면 클로드 코드가 실행 |

- 기본값은 커밋·푸시를 하지 않는 것이다. 작업을 마치면 커밋 지점만 표시한다.
- 사람이 "커밋해줘", "푸시해줘"라고 지시하면 그때 실행한다. 지시 없이 먼저 하지 않는다.
- 푸시 전에는 반드시 추적 대상 파일에 비밀값(.env, 키, PIN, 입장 코드)이 없는지 확인한다.
- 커밋 메시지에 Co-Authored-By 를 남겨 누가 실행했는지 저장소에 드러나게 한다.
- 코덱스 검토 결과의 수용/거부 기록은 /making 페이지에 공개한다.
- 오늘 밤 제작에는 TDD를 적용하지 않는다(시간 제약). TDD는 수강생에게 가르칠 방법론이다.

## 작업 방식

- 기능별로 순차 구현하고, 각 기능 완료 시 브라우저 테스트로 검증한 뒤 다음으로 넘어간다.
- 각 기능 완료 시점을 커밋 지점으로 명시한다(커밋은 사람이).
- 개발 단계는 PRD의 P0 → P1 → P2 순서를 따른다. P0가 끝나기 전에 P1을 시작하지 않는다.

## 기준 문서

| 문서 | 용도 |
|------|------|
| `PRD-해커톤-베이스캠프.md` | 요구사항의 단일 출처 (v2.3). §7.5 와 §10 에는 옛 버전 기록이 남아 있고 각 자리에 표시해 두었다 |
| `원고-실습콘텐츠-이승엽파트.md` | 미션 8종 시딩 콘텐츠(§B), 운영 체크(§C) |
| `DESIGN.md` | 디자인 토큰·컴포넌트 스펙 (2001년 닌텐도닷컴 콘솔 크롬) |
| `COPY.md` | UI 문구 원칙 (im-not-ai) |
| `docs/plans/` | 구현 계획 문서. 본문은 만들 당시 기록이라 다크 토큰과 Pretendard 서술이 남아 있다. 지금 기준이 아니다 |

## 기술 스택

- Next.js(App Router) + React + Tailwind CSS
- Firebase Firestore (Storage 불필요 — 모든 제출은 텍스트·링크)
- 배포: Vercel (배포 실행은 사람)
- 폰트: Arial, 한글은 돋움. 웹폰트를 쓰지 않는다.

## 디자인 하드 룰 (상세는 DESIGN.md — 2001년 닌텐도닷컴 콘솔 크롬)

- 페이지는 조립된 기계다. 모든 영역은 베벨 판(`.plate`)으로 만든다. 위는 밝게, 아래는 `#3d4f97` 그림자선.
- 본체는 페리윙클 `#7a8aba`. 한 단 올라간 판은 `#8ba1d4`, 인셋은 `#dedede`, 흰 카드는 `#ffffff`.
- 명령 레이어는 카본 `#21242e`에 하프톤 점무늬(`.slab`). 상단바, 운영자 머리띠, 어두운 버튼이 여기 속한다.
- 따뜻한 색은 길안내에만 쓴다. 메뉴 글자 `#e48600`, 도구 버튼과 배지 `#ecab37`, 전진과 제출 `#f68d1f`. 차가운 크롬에는 액션 색을 칠하지 않는다.
- 모서리는 기본이 각지다. 둥근 것은 로고 알약, 원형 화살표 배지, 라디오 점에만.
- 그림자 흐림 금지. 깊이는 하드 베벨로만 만든다.
- 구조 라벨은 대문자 Arial Bold 11px, 자간 0.5px(`.chrome-label`).
- 히어로 워드마크는 외곽선과 딱딱한 그림자를 반드시 넣는다(`.wordmark`). 평평한 글자로 두지 않는다.
- 페이지 색조는 히어로 필드에만: 홈 라벤더 `#acace7`, 광장 아이스 `#c0d5e6`, 검토 게이트와 발표 `#a7282b`, 운영자 `#206479`.
- 글꼴은 Arial, 한글은 돋움. 웹폰트를 쓰지 않는다.
- 여백을 넓히지 않는다. 판이 맞붙은 밀도가 이 디자인이다.
- 버튼 최소 높이 44px, 표의 기호 버튼도 44px 정사각. 원본에는 없던 요구지만 노트북과 태블릿을 함께 쓰는 자리라 지킨다.
- 오렌지와 앰버 위에는 카본 글자를 올린다. 흰 글자는 대비가 2.4:1 로 읽히지 않는다.

### 원본과 다르게 가는 부분

- **히어로에 사진을 넣지 않는다.** DESIGN.md 는 full-bleed 사진 필드를 요구하지만, 이 앱에는 쓸 사진이 없고 참가자 화면에 임의 이미지를 넣는 것이 수업에 도움이 되지 않는다. 단색 페이지 색조 위에 외곽선 워드마크를 올리는 것으로 대신한다.
- 이 예외를 늘리지 않는다. 다른 규칙을 어길 때는 여기에 이유와 함께 적는다.

## 문구 하드 룰 (상세는 COPY.md)

- 본문 볼드 금지. 이모지는 막혔어요 버튼(🙋) 한 곳만.
- 대시(—) 부가 설명 금지. 번역투 금지. 단언형 종결.
- 같은 종결어미 3연속 금지. 안내문 2~4문장. 오류 메시지는 원인 1문장 + 다음 행동 1문장.
- 프롬프트 카드 원문(원고 §B)은 문구 원칙 적용 대상이 아니다. 글자 그대로 시딩한다.

## 보안·개인정보

- 본인 `progress` 문서만 쓰기. `roster`·`missions`·`config`는 클라이언트 읽기 전용.
- 운영자 기능은 PIN 검증 `/api/admin/*` 서버 라우트 경유. PIN은 환경 변수 `TEACHER_PIN`.
- 수집 정보는 이름·소속만. 종료 후 데이터 삭제 절차를 README에 명시한다.
- 제출물에 학생 실명 입력 금지 안내를 글 쓰는 화면(미션 폼)에 스크롤을 따라오도록 고정 노출한다.
