# PRD — 해커톤 베이스캠프 (2026 바이브 코딩 역량강화 워크숍 LMS)

**버전**: v2.2 (2026-08-16) — 개인별 입장 코드를 없애고 워크숍 전체가 하나의 수업 코드를 쓴다(§2.1, §4, §8). v2.1(2026-08-15)에서 제작 파이프라인(클로드×코덱스 교차)과 수강생 파이프라인(클로드 코드 + TDD)을 분리했다.
**작성**: 이승엽 (장평중 정보교사) × Claude
**목표**: 8/16~17 워크숍에서 이승엽 담당 세션(1일차 사전작업 안내, 2일차 스프린트 1~2)의 진행·제출·검토를 관리하는 LMS.
**오늘의 최우선 목표**: **내일(1일차 16시)과 모레 오전(스프린트 1~2)이 완벽하게 돌아가는 것.**
**메타 포인트**: 이 플랫폼 자체가 바이브 코딩 산출물 — /making에서 제작 파이프라인을 공개한다.

---

## 0. 배경과 철학

- 대상: 중·고 정보 교사 수강생 17명 + 강사 5명 (총 22명, 전원 성인).
- 슬로건: **"AI는 비결정적으로 생성하고, 교사는 결정적으로 검증한다."**

### 이 플랫폼의 제작 파이프라인 (오늘 밤, 나의 작업 규율 — /making에 공개)
- **기획 — 클로드** (이 PRD)
- **구현 — 클로드 코드**
- **검토 — 코덱스** (교차 AI 검토: 다른 모델의 눈으로)
- **최종 결정 — 나** (검토 의견의 수용/거부를 판단)
- **푸시 — 나** (커밋·푸시는 사람이)
- ※ 오늘 밤 제작에는 TDD를 적용하지 않는다(시간 제약). TDD는 내일 수강생에게 가르칠 방법론.

### 수강생의 해커톤 파이프라인 (내일, 미션 구조의 근거)
- 도구는 **클로드 코드 하나만** 사용한다(코덱스 없음).
- 기획·구현·검토 모두 클로드와 하되, **검토는 새 대화(만든 맥락 밖의 눈)**에서 검토자 역할 프롬프트로.
- 구현은 **TDD 3단**: 테스트 코드(RED) → 구현·통과(GREEN) → 리팩토링(REFACTOR).
- **최종 결정과 커밋·푸시는 본인이.**
- 2일차 해커톤 주제: "나의 정보 수업을 바꿀 수업 도구 만들기" — 개인전.
- **스텝 개방 원칙**: 한 번 열린 미션은 닫지 않는다. 이전 스텝~현재 스텝을 자유롭게 오가며 수정(뒤 스텝은 운영자가 열기 전까지 잠금).

---

## 1. 기술 스택

- **제작**: Claude Code(구현) + Codex(검토) / **배포·푸시**: 사람
- **프론트**: Next.js(App Router) + React + Tailwind
- **백엔드**: Firebase — Firestore(제출·진행 기록). Storage 불필요(모든 제출은 텍스트·링크).
- **배포**: Vercel
- (P2) Claude API — 서버 라우트 경유 계획 자동 검토. P0에서는 프롬프트 카드 복사 방식.

## 2. 사용자 시나리오

### 2.1 수강생(교사)
1. 배포 URL 접속. **이름 + 수업 코드 → 사전 명단 대조** 입장(코드는 참가자 전원이 같은 것을 쓰고, localStorage 로 복구). 이미 다른 기기가 쓰는 이름은 막히고, 운영자가 이름 풀기를 눌러 넘긴다.
2. 홈 상단에 **파이프라인 스텝퍼**: m1→m8 전 단계가 한 줄로 보이고, 열린 단계는 언제든 클릭해 돌아가 수정 가능. 잠긴 단계는 자물쇠.
3. 미션 폼 작성(1.5초 디바운스 자동 저장, 제출 후에도 수정 가능). 이전 미션의 내 제출물이 다음 미션 상단에 자동 표시(m2→m3, m3→m4, m4→m6 …).
4. 검토 게이트 미션(m5, m8)에서는 AI 검토(클로드 새 대화) 결과와 **수용/거부 결정**을 기록.
5. **광장(현황판)**에서 동료들의 파이프라인 위치·공개 제출물 열람, 동료 검토·피드백 작성.
6. 막히면 "막혔어요 🙋" 버튼 → 운영자 대시보드에 표시.

### 2.2 운영자(이승엽)
1. `/teacher`, PIN 입장.
2. 실시간 그리드: 22명 × 8미션 파이프라인 위치(onSnapshot).
3. **미션 열기 전용 토글** — 세션 진행에 맞춰 순차 오픈. 닫기 버튼은 비상용으로만(기본 UX는 누적 개방).
4. 막혔어요 큐, 제출물 열람, 동료 검토 셔플 배정, 발표 순서 지정.
5. `/present` 발표 모드(m7·m8 제출물 기반).

### 2.3 다른 강사(보조)
- 동일 입장, roster `role: staff`, [강사] 배지, 제출 의무 없음.

## 3. 기능 — 미션 엔진 (파이프라인형)

미션은 Firestore `missions` 컬렉션에서 읽어 렌더링. 콘텐츠만 바꾸면 새 미션이 동작해야 한다.

미션 공통 스키마: 제목, 단계 라벨(스텝퍼 표기용), 안내문(마크다운), 폼 필드 배열(text/textarea/url/select/checklist), 열림 상태(**open 이후 상시 개방**), 제출물 공개 범위(비공개/이름만/전체 공개), `carryover`: 이전 미션 특정 필드를 상단에 표시.

시딩할 미션 8종(콘텐츠는 '원고·실습 콘텐츠' §B 참조):

| id | 단계 | 미션 | 세션 | 수행 주체 |
|----|------|------|------|-----------|
| m1 | 발견 | 내 수업 병목 찾기 | 1일차 16시 | 사람 |
| m2 | 발견 | 아이디어 한 줄 | 1일차 숙제 | 사람 |
| m3 | 기획 | PRD 작성 | 스프린트 1 | 클로드와 대화 |
| m4 | 기획 | 구현 계획(시나리오·와이어프레임·테스트 케이스 설계) | 스프린트 1 | 클로드와 대화 |
| m5 | 검토① | 계획 검토 게이트(클로드 새 대화 검토 + 동료 검토 + 내 결정) | 스프린트 1 | 클로드+사람 |
| m6 | 구현 1차 | 테스트 코드 작성(RED) | 스프린트 2 | 클로드 코드 |
| m7 | 구현 2차 | 기능 구현·테스트 통과(GREEN) + 배포 | 스프린트 2 | 클로드 코드 |
| m8 | 구현 3차 | 리팩토링(REFACTOR) + 코드 검토 게이트 + 커밋·푸시 | 스프린트 2~3 | 클로드+사람 |

- m5 동료 검토: 운영자 셔플 배정(짝 없는 1명은 3인조). 검토자는 상대의 m3·m4 전문 열람.
- m7 배포 URL 제출 → 광장 실시간 공개, `/present`의 데이터 소스.
- m8은 스프린트 3(오후, 기능 개선)과 겹쳐 진행 가능 — 열어 두면 오후에도 이어짐.

## 4. 데이터 모델 (Firestore)

※ v2.2(8/16) 변경: 개인별 입장 코드를 없애고 워크숍 전체가 하나의 수업 코드(`WORKSHOP_CODE` 환경 변수)를 쓴다. `codes` 컬렉션은 쓰지 않는다. 이름 선점을 막기 위해 `progress.ownerUid` 가 이미 다른 기기의 것이면 입장이 409 로 막히고, 운영자가 `/teacher` 에서 이름 풀기를 눌러야 넘어간다.

```
roster/{name}
  ├ name, school, role: "student"|"staff"

progress/{name}
  ├ ownerUid                // 지금 이 이름을 쓰는 기기. /api/enter 만 기록한다
  ├ missions: { m1: {status, updatedAt, data{...}}, ... }
  ├ currentStep            // 스텝퍼 하이라이트용, 제출 시 자동 갱신
  ├ stuck: bool
  └ reviewTarget: name     // m5 동료 검토 배정 상대

reviews/{reviewer}_{target}
  ├ checklist{...}, comment, createdAt

missions/{id}
  ├ title, stepLabel, order, session, guide(md)
  ├ fields: [ {key, label, type, placeholder, options?} ]
  ├ carryover: [ {fromMission, fromKey, label} ]
  ├ open: bool             // true가 된 뒤에는 계속 true 유지
  └ visibility: "private"|"name"|"public"

config/global
  ├ presentOrder: [names]
```

## 5. 보안 규칙 요점

- 본인 `progress` 문서만 쓰기. `roster`·`missions`·`config` 읽기 전용. 동료 검토는 `reviews/{reviewer}_{target}`에만 쓰기(reviewer=본인 검증).
- 운영자 기능(미션 오픈·배정·발표 순서)은 PIN 검증 서버 라우트 경유.
- 개인정보: 이름·소속만 수집. 종료 후 데이터 삭제 절차 README 명시. 제출물에 학생 실명 입력 금지 안내 고정 노출.

## 6. 라우트

| 라우트 | 설명 |
|--------|------|
| `/` | 입장(이름) → 홈(파이프라인 스텝퍼 + 미션 목록) |
| `/mission/[id]` | 미션 폼(carryover 표시 포함) |
| `/plaza` | 광장(파이프라인 현황 + 공개 제출물) |
| `/making` | 이렇게 만들었어요(이 사이트의 PRD와 제작 파이프라인 — 클로드 구현·코덱스 검토·사람 결정 기록 공개) |
| `/teacher` | 운영자 대시보드(PIN) |
| `/present` | 발표 모드(m7·m8 기반) |
| `/api/admin/*` | 미션 오픈·셔플·발표 순서(PIN 검증) |

## 7. 비기능 요구사항

- 동시 접속 22명, 실시간 반영 1초 이내(onSnapshot).
- 폼 자동 저장(1.5초 디바운스), 재접속 복구, **모든 열린 미션은 항상 재수정 가능**.
- 스텝퍼는 모바일에서 가로 스크롤. 한국어 UI. 시각 언어는 §7.5 디자인 시스템을 따름.
- 오류: 저장 실패 시 명확한 안내 + 로컬 임시 보관.

## 7.5 디자인 시스템 — DESIGN-framer.md 적용

저장소 루트에 `DESIGN.md`(첨부한 DESIGN-framer.md)를 넣고, 모든 UI는 그 토큰을 따른다. 다크 온리 — 라이트 모드 없음. 아래는 이 앱에 맞춘 매핑.

### 표면과 색
- 페이지 배경 `canvas #090909`. 미션 카드·보조 버튼·입력 필드는 `surface-1 #141414`, **현재 진행 중인 미션 카드와 선택 상태는 `surface-2 #1c1c1c`** — 강조는 색이 아니라 표면 승급으로.
- `accent-blue #0099ff`는 하이퍼링크·포커스 링(`rgba(0,153,255,0.15) 0 0 0 1px`)·선택 표시에만. 배경·버튼 채움 금지.
- 텍스트 위계는 `ink #ffffff` / `ink-muted #999999` 둘뿐. 중간 회색 추가 금지.
- 완료 체크 글리프만 `semantic-success #22c55e`(글리프 채움, 표면 금지).

### 그라디언트 스포트라이트 (절제해서, 화면당 1~2개)
- **스텝퍼의 현재 스텝 칸**: `gradient-violet #6a4cf5` — 파이프라인에서 "지금 여기"가 유일하게 빛나는 칸.
- **검토 게이트 미션 카드(m5·m8)**: `gradient-magenta #d44df0` — 게이트임을 표면으로 구분.
- **막혔어요 상태 배지·운영자 큐 하이라이트**: `gradient-coral #ff5577`.
- 그 외 표면에 그라디언트 금지. 섹션 배경 그라디언트 금지(그라디언트는 카드다).

### 타이포그래피 (한글 치환)
- GT Walsheim·Inter는 한글 미지원 — **디스플레이·본문 모두 Pretendard Variable**로 치환(라틴·숫자는 Pretendard 내장 라틴이 Inter 계열이라 스펙의 목소리가 유지됨). 숫자 강조 구간(카운트다운·통계)은 `tnum` 활성.
- 자간 원칙 유지하되 한글 보정: 디스플레이는 **-0.03em**(스펙의 -5% 대신 — 한글은 그 이상 조이면 뭉침), 본문 -0.01em. 크기 대비로 위계 표현, 굵기는 500~700 좁은 밴드.
- 화면별 디스플레이: 홈 히어로(워크숍명) display-lg급 하나만. 미션 제목 display-md급. 나머지는 body 위계.

### 컴포넌트 매핑
- **제출·주요 CTA**: 흰 필 버튼(`button-primary` — 흰 배경 + 검정 텍스트, `rounded.pill`). 보조 액션은 차콜 필(`button-secondary`). 고스트·보더 버튼 금지.
- **입력 필드**: `text-input`(surface-1, rounded 10px) + 포커스 블루 링.
- **미션 카드**: `pricing-card` 문법(surface-1, rounded 20px, padding 24px). 현재 미션은 `-featured`(surface-2).
- **스텝퍼**: 완료 칸 ink, 잠긴 칸 ink-muted + 자물쇠, 현재 칸 violet 스포트라이트. 칸은 `rounded.md`.
- **광장 그리드**: `template-card`(surface-1, rounded 15px, padding 12px), 데스크톱 2열 → 810px 이하 1열.
- **운영자 그리드**: `comparison-row` 문법 — canvas 배경, ink-muted 데이터, `hairline-soft #1a1a1a` 1px 밑줄.
- **상단 바**: `top-nav` 56px, canvas 배경, 우측에 내 이름 배지(차콜 필).

## 7.6 문구 원칙 — im-not-ai(humanize-korean) 적용

앱에 들어가는 모든 문구(미션 안내문, 버튼, 빈 상태, 오류 메시지, 토스트)는 AI 티 패턴을 배제한다. 저장소 루트에 `COPY.md`로 아래 원칙을 넣고 문구 생성 시 참조시킨다. (원본 스킬: github.com/epoko77-ai/im-not-ai — quick-rules 발췌)

- 본문 볼드 금지, 이모지는 UI 어포던스 한 곳(막혔어요 버튼)만. 강조는 문장 순서로.
- 대시(—) 부가 설명 금지. 쉼표를 쓰거나 문장을 나눈다.
- 번역투 금지: "~에 대해", "~를 통해", "~에 있어", "~와 관련하여", 이중 피동("~되어진다").
- "~할 수 있습니다"보다 단언: "저장할 수 있습니다" 대신 "저장됩니다".
- 같은 종결어미 3연속 금지. 합니다체로 통일하되 어미를 바꿔 가며.
- "A가 아니라 B" 대구는 화면당 1회. 열거 도입구("다음과 같은", "크게 세 가지") 없이 바로 본론.
- 과장 어휘(혁신적·강력한·손쉽게) 금지. 무엇이 어떻게 되는지 구체 동사로.
- 안내문은 2~4문장. 오류 메시지는 원인 한 문장 + 다음 행동 한 문장.
- 수치·고유명사·프롬프트 카드 원문은 손대지 않는다.

## 8. 환경 변수

```
NEXT_PUBLIC_FIREBASE_*
FIREBASE_ADMIN_*
TEACHER_PIN
WORKSHOP_CODE            # 참가자 전원이 함께 쓰는 수업 코드
FALLBACK_REVIEWER_NAME   # 학생이 홀수일 때 짝 없는 한 명을 검토할 계정
```

## 9. 개발 단계

**P0 — 오늘 (필수)**
1. 셋업 + Firebase + 입장(명단 대조, localStorage)
2. 파이프라인 미션 엔진(스텝퍼 + carryover + 누적 개방) + 미션 8종 시딩
3. 광장 + 막혔어요
4. 운영자 대시보드(그리드·오픈 토글·셔플 배정)
5. /making + 배포 + roster 22명 시딩
6. **코덱스 검토 1회**(구현 완료 후 저장소 검토 → 수용/거부 기록을 /making에 남김) → 내가 커밋·푸시

**P1 — 내일 밤 여유 있으면**
- /present 발표 모드 · 회고 미션(m9) 시딩 · m8 검토 결과의 광장 하이라이트

**P2 — 추후**
- Claude API 자동 계획 검토 · 결과물 아카이브(연구회 공유용) · CSV 내보내기

## 10. Claude Code 빌드 시작 지시 (첫 메시지용)

> "Next.js(App Router) + React + Tailwind + Firebase(Firestore) 기반으로 '해커톤 베이스캠프'라는 교사 워크숍(22명, 2일) LMS를 만들 거야. 배포는 Vercel. 참가자는 이름으로 입장(사전 명단 대조, localStorage 복구, 비밀번호 없음). 홈 상단에 m1~m8 파이프라인 스텝퍼가 있고, 운영자가 연 미션은 **닫히지 않고 누적 개방**되어 참가자가 이전 단계로 자유롭게 돌아가 수정할 수 있어(잠긴 단계는 자물쇠). 미션은 Firestore missions 컬렉션에서 읽어 렌더링하고 콘텐츠만 바꾸면 동작해야 하며, carryover 설정으로 이전 미션의 내 제출물이 폼 상단에 표시돼. 미션 8종을 시딩해줘: 병목 찾기 → 아이디어 한 줄 → PRD 작성 → 구현 계획(사용자 시나리오·텍스트 와이어프레임·테스트 케이스 설계) → 계획 검토 게이트(AI 검토 결과의 수용/거부와 이유 기록, 동료 검토는 운영자 셔플 배정으로 reviews 컬렉션에 체크리스트+코멘트) → 테스트 코드 RED → 구현·배포 GREEN(배포 URL·깃허브 URL 제출) → 리팩토링·코드 검토 게이트·커밋 푸시 확인. 폼은 1.5초 디바운스 자동 저장, 제출 후 수정 가능. 광장(/plaza)은 각자의 파이프라인 위치와 공개 제출물, '막혔어요' 버튼. 운영자 대시보드(/teacher, PIN)는 onSnapshot 실시간 22명×8미션 그리드, 미션 오픈 토글(열기 전용, 닫기는 비상용), 셔플 배정, 막혔어요 큐. /making에 이 PRD 원문과 제작 파이프라인 설명을 공개해줘. 운영자 기능은 PIN 검증 /api/admin 경유. 디자인은 저장소 루트의 DESIGN.md를 따라줘 — 다크 온리(#090909 캔버스), 흰 필 CTA, 표면 승급(surface-1/2)으로 위계, 링크·포커스에만 #0099ff, 그라디언트는 화면당 1~2개 카드(현재 스텝 violet, 검토 게이트 magenta, 막혔어요 coral)로만. 폰트는 GT Walsheim·Inter 대신 전부 Pretendard Variable, 디스플레이 자간 -0.03em. UI 문구는 저장소 루트의 COPY.md를 따라줘 — 본문 볼드·이모지 금지(막혔어요 버튼만 예외), 대시 부가설명 금지, 번역투 금지, 단언형 종결, 같은 어미 3연속 금지. Firestore 보안 규칙(본인 progress만 쓰기, reviews는 reviewer 본인만), .env.example, 종료 후 데이터 삭제 절차 README까지. 커밋과 푸시는 내가 직접 할 테니 너는 푸시하지 마."
